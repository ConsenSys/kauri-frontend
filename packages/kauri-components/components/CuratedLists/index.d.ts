import React from "react";

const RenderCardContent: (
  additionalProps: { fromAdmin: boolean; Link: React.ReactNode }
) => (resourceRowProps: any) => React.ReactElement<any>;

export { RenderCardContent };
